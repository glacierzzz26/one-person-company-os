# Phase 8.2 归档 — 执行委派首个闭环(writer)+ 判读回网关

> 子阶段落地归档(2026-09-04)。实施契约见 [agent-delegation-loop.md](../design/agent-delegation-loop.md)(含 §八 实施修订记录),方向见 [model-runtime.md](../design/model-runtime.md)(修订 B:执行 = 委派集成 agent CLI)。
> 定稿门通过 → 按契约实施 → 验收全绿 → 本子阶段冻结。后续续接只看 进度总表 + 本文档,不靠记忆。

## 交付物

两条生产接线(修订 B 首个闭环)+ 一处审计查询补口,无迁移/无 sqlc/无 go.mod 变更:

| 文件 | 内容 | 说明 |
|---|---|---|
| `internal/service/delegate.go`(新) | `Delegator/DelegateSpec/DelegateResult` 接口 + `claudeDelegator`(`claude -p … --output-format json --permission-mode bypassPermissions --permission-prompts none`,cwd 钉 spec.Workspace,env = os.Environ + ModelEnv);git 助手 `gitDirCmd/wsIsGit/wsPorcelainClean/captureChanges`(固定命令,不经用户串);`delegateWriter`(live writer:委派 → git 捕获真实 diff → 空 diff 显式报错 → `eng_delegate` 审计);`delegateBaseline`(认领起点前置);`delegateEnv`(writer_endpoint_id 可选 cheap 配对 → `ANTHROPIC_*`);`delegateTimeout`(min(任务剩余, 20m));`delegateAuditDetail`;`delegateBrief`(有界任务简报:workspace/禁 commit/自测/治理文件禁改) | 返回 = 真实 diff 文本,延续 runEngPhase 语义(test/review 判读照旧消费) |
| `internal/service/engine.go`(改) | `modelCall` 由 `ClaudeCLI.Generate` 切 **OpenAI 兼容网关 `Chat` 单轮文本**(proto=openai 门 + `finish_reason=length` 显式截断错误);`engCall` live writer 分支 → `delegateWriter`(不再文本产出);头注同步(判读=网关文本、writer=委派) | planner/triage/test/review 判读统一回网关(6.2+6.3 共用调用点不变,签名不变) |
| `internal/service/driver.go`(改) | round 循环前加 live 委派前置调用(`!humanOverride` 时 `delegateBaseline` → 失败 `engFail(writer)`);回合/熔断/审计语义逐字不动 | 熔断续跑(humanOverride)跳过 —— 残留 = 本任务先前产物 |
| `internal/service/service.go`(改) | `Service` 增 `delegator Delegator` 字段;`New` 默认 `&claudeDelegator{}` | 测试注入缝 |
| `internal/storage/repository/audit.go`(改) | 新增 `HasAuditAction(entity_type, entity_id, action)` 存在性查询(手写参数化 SQL,镜像 memory.go 风格) | §八 修订记录 1 的判别依据:「从未委派 vs 自己的残留」 |
| `internal/provider/claudecli.go`(改注) | 头注「委派属 8.5」改口为 8.2 `claudeDelegator`(claude 二进制 agent 模式,同 env 语义);Generate 仍 Deprecated、代码零改动 | |
| `internal/service/delegate_test.go`(新) | 用例 1–4 + 空 diff + baseline 判别(见下) | fake delegator + 假网关 harness |
| `internal/service/driver_test.go`(新) | 用例 5–6(scripted 回归闭环 + writer 前置失败);补齐 6.2 起 driver 无 service 级单测的空缺 | |

## 验证

- `gofmt` 本次改动文件干净(仓库其余历史非 gofmt 文件为改动前既有,未动);`go vet ./...` 干净;`go build ./...` 通过。
- `go test ./...` 全绿;**0-7 与 8.1 既有测试零改动**(console/server/provider/notify/service 原样通过)。
- 新增用例全绿:
  - 用例 1 `TestGitHelpers`:git 助手三态(新/改/删)捕获 + clean 判别 + 非 git 判定。
  - 用例 2 `TestDelegateWriterRoundLoopGateway`:engineering child task(git ws + reviewer=假网关 openai)→ live 委派闭环跑通 writer→test→review:writer execution 完成、结果含真实 diff(fake 写入文件在 diff 内)、audit `eng_delegate` 存在(actor=`agent:none`,detail 前缀 `claude round=0 retry=0 ws=`);假网关收到 POST `/v1/chat/completions` + `Bearer sk-test-123`(证判读切网关);task completed 且 result=最终 diff。
  - 用例 3 `TestModelCallProtoGate`:anthropic/auto 端点 → error 含 `proto=openai`;openai 网关正常回 Content。
  - 用例 4 `TestModelCallLengthTruncation`:假网关 `finish_reason:"length"` → error 含 truncated(不静默当成功)。
  - 用例 5 `TestScriptedRoundLoopRegression`:`OS_ENGINE_MODE=scripted` + pass/approve 跑最小一轮 → completed、结果含确定性假 diff;fake delegator 调用数 0(委派零命中)。
  - 用例 6 `TestDelegateBaselineFailsViaExecute`:workspace 非 git → failed(last_error 含 git workspace);脏起点无委派 → failed(含 has not delegated before)。对照 `TestDelegateBaselineCleanPassesToWriter` + `TestDelegateBaselinePriorDelegationAllowsDirty`:干净放行走委派;脏但有本任务 eng_delegate → 放行(返工语义,自锁防护)。

## 实施期修订(相对冻结契约,详见 agent-delegation-loop.md §八)

1. **认领起点 clean 前置判别细化**:脏但已有本任务 `eng_delegate` 审计 → 放行(残留 = 本任务先前 attempt/返工产物,累积迭代);否则硬错误。原因:无条件脏即失败会自锁回合机自有 rework/熔断续跑/requeue。
2. **文件清单追加** `internal/storage/repository/audit.go`(改):`HasAuditAction` 存在性查询(手写 SQL,无迁移/sqlc)。
3. **接口口径**:`DelegateResult.Diff` 真实实现留空,产出统一由 `delegateWriter.captureChanges` 捕获。

## 未做 / 留给后续

- **真实 claude Code live 委派验收(用户)**:真实授权下委派自主读/改/跑 → OS 收真实 diff → 网关判读闭环;`--permission-mode bypassPermissions` 等 flags 按本机 `claude --help` 复核;cheap 配对需 Anthropic 方言端点(claude 自带经济模型或 Anthropic 兼容网关)。
- **8.3 接线点**:agent CLI 工具族注册选型(claude→codex);委派边界收紧(allowlist/受限 Bash/沙箱);planner split 子任务共享工作树仲裁(8.2 范围 = 单条原子工程任务绑定自己 git workspace);判读结构化(替换 parse* 正则、返工喂 test 失败原因);多任务同库仲裁。
- **8.4**:`endpoint.tier` 档位强制(migration 0010)+ 角色默认映射落建单。

## 冻结

8.2 冻结 ✅(2026-09-04)。续接点:8.3 agent CLI 工具族 + 委派边界 + 判读结构化 —— 见 model-runtime.md §六。
