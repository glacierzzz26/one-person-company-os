# One-Person Company OS — 项目详解

> 全局背景、数据流、技术栈与边界。续接阶段工作前先读本页 + [进度总表](进度总表.md)。

## 定位

构建一个由**一个人负责最终决策、AI Agent 承担大量执行**的公司操作系统(Company Operating System)。不是超级 Chatbot、不是 Agent Framework、不是纯 Workflow Engine、不是 ERP。

方向基线(最终版,不轻易改动):`docs/../One-Person-Company-OS-Design-v1.0.md`(内容 v1.1)。

## 核心模型(v1.1 修正)

- **Agent 是 Capability 中的执行角色**(组织/角色维度):`Company → Capability → Agent`
- **Workflow 是跨 Capability 的流程编排**(流程/方法维度):`Workflow → Task → Execution → Runtime → Tool`
- 二者不在同一层级。Verification / Approval 属于**治理链**,按风险策略触发,不是每次执行必经。
- 治理:Policy 由系统强制实施,Agent 在受控环境无法直接改 Policy/Permission、删 Audit、绕过 Approval。

## 技术栈

| 维度 | 选择 |
|---|---|
| 语言 / 架构 | Go · Modular Monolith(单二进制) |
| CLI | Cobra |
| DB / SQL | SQLite · SQLC · golang-migrate |
| 配置 | YAML |
| 日志 | slog |
| Sandbox / 队列 | Docker(Phase 1)· SQLite-backed Queue(Phase 1) |
| Web 控制台(7.3) | React 18 · TypeScript · AntD v5 SPA · Vite · react-router v6;`go:embed` 单二进制同源托管(`os server`);dev 用 Vite proxy(免 CORS) |

## 开发线

- `dev`:主开发线,Phase 0-6 已全部合入;新工作一律从 dev 拉功能分支
- `phase-N-*`:阶段功能分支,从 dev 拉出、测试通过后合回 dev(分支 ref 保留在合并点,不再推进)
- `master`:发布线,只从 dev 来(暂未建立)

## 阶段

| 阶段 | 定位 | 状态 |
|---|---|---|
| Phase 0 | Company Kernel | ✅ |
| Phase 1 | Execution Kernel | ✅ |
| Phase 2 | Engineering Capability | ✅ |
| Phase 3 | Research / Product | ✅ |
| Phase 4 | Business Capabilities | ✅ |
| Phase 5 | One-Person Company OS | ✅ |
| Phase 6 | 研发部(Engineering)能力就位(模型池真实现 + 回合驱动 + 两路接活 + 服务化通道 + 运营视图/飞书触达) | ✅ |
| Phase 7 | 运营控制台(7.1 `/api/v1` JSON API ✅ · 7.2 UI 方向/高保真原型定稿 ✅ · 7.3 Web 控制台 React+TS+AntD SPA 实现) | 🔶 |
| Phase 8 | 模型运行时:多厂商 tool-use + 角色分层(claude 降为池中工具;高智拆/审、经济写) | 🔶 |

> 当前:Phase 7.3 Web 控制台按原型实现、8 视图全量接入 `/api/v1` 真实契约(非 mock),tsc/vite build 干净、
> `go build ./...` + `go test ./...` 绿、单二进制内嵌 SPA smoke 通过;收口 dev 分支、文档同步/归档进行中
> (2026-09-03)。Phase 6 本地离线验收全通过;真实 飞书/Claude/GitHub live 验收待用户。
> (2026-09-04)Phase 8 模型运行时方向设计定稿(`docs/phase8/design/model-runtime.md`):Provider 升级
> `messages+tools` 多厂商 HTTP、阶段原生 tool-call、端点 `tier` 三档 + 角色默认映射(planner/review=高智、
> writer=经济)、claude 降为池中可调用工具;实施 8.1–8.5 待立项。
> 本阶段方向:Web 运营控制台(一人操作台)——CLI `os overview` 曾为唯一全景入口;Phase 7.1 已交付 `/api/v1`
> JSON 读/写 API(契约见 `docs/phase7/design/web-console.md`),7.2 定稿 UI 方向(console-ui.md + 高保真原型),
> 7.3 起按原型实现 React+TS+AntD SPA,全部接真实端点、`go:embed` 与 `os server` 同源部署;dev 期间
> `cd web && npm run dev`(proxy → 127.0.0.1:8787)热更,`make ui` 构建产物拷入内嵌目录。
