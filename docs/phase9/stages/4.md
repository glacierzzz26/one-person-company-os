# Phase 9.4 归档 — CLI 只读收口:env seam 反向门控 + server 业务 flag 下线 + 残留文案清理

> 子阶段落地归档(2026-09-07)。实施契约见 [cli-readonly.md](../design/cli-readonly.md)(定稿),方向见 [config-governance.md](../design/config-governance.md)(定稿,8 项决策)。
> 定稿门(2026-09-07,AskUserQuestion 三项决策:① env seam 门控 = 反向默认(测试开、产品恒关);② CLI 收口深度 = 收 env/flag + 文案清理,领域写命令全保留;③ server 业务 flag 删除)→ 按契约实施 → 验收全绿 → 本子阶段冻结。
> 落地后工作树 = 9.4 未提交;9.3 已于 commit `a84ecce` 收口(本阶段立足点)。**Phase 9 至此完结。**

## 交付物

| 文件 | 内容 | 说明 |
|---|---|---|
| `internal/service/seam.go`(新) | 包级 `envSeam = false` + `SetEnvSeam(on bool)` + 头注(决策①体例:默认关 = 产品语义,恒不读 OS_* 配置 env;仅 go test TestMain 打开;非测试代码不得调用) | 反向门控总闸 |
| `internal/service/runtime.go`(改) | `engineScripted`/`agentCLI`/`issueSourceFor` 三处 env 分支收进 `envSeam` 门(seam 关 → 纯 DB company→global→默认;seam 开 → 现 env-first 行为不变);头注更新;`issueSourceFromEnv` 标注「9.4 起仅测试 seam 打开时被调用,产品不可达」 | 产品恒不读 env 的结构性兑现(方向 §八.1) |
| `internal/service/delegate.go`(改) | `agentCLIFromEnv`(产品死代码,仅测试直测)加 `envSeam` 门,seam 关 → 缺省 claude;:47/:285 注释改「family=s.agentCLI(DB 生效)/env 仅测试 seam」 | |
| `internal/endpoint/seal.go`(改) | 平行 `envSeam` + `SetEnvSeam`;`envKey()` 门控:seam 关 → 不再读 `OS_ENDPOINT_KEY`,报 `endpoint master key not loaded (<db>.key); run /setup`(文案不含 env 名);seam 开 → 现行为不变 | 主密钥缺失兜底关闭 |
| `internal/service/testmain_test.go`(新) | TestMain 开 service + endpoint 两 seam(service 用例经 endpoint envKey 造旧 key) | 600+ 既有测试零 body 改动 |
| `internal/endpoint/testmain_test.go`(新) | TestMain 开 endpoint seam | |
| `internal/cli/serverconfig.go`(改) | 删 `flagVals`/`changed` 参数;`effectiveServerConfig` → `serverConfigFromApp(app settings.AppSetting)`(纯函数):零值行 = 零填内置默认(HTTPPort/PollMin/QueueIntervalSec → `settings.Default*`),digest ""/"off"(大小写)→ "";bounds 只来自非零越界行值(port 1..65535、poll≥1、interval≥1) | §3.2 权威源 = Web 设置页 |
| `internal/cli/server.go`(改) | 删 5 业务 flag 定义/声明(`--port/--poll/--digest/--queue-work/--queue-interval`);RunE `effectiveServerConfig(app, flagVals{…}, Changed)` → `serverConfigFromApp(app)`;保留 `--db/--config`(root)+ `--digest-now`(boot 一次性触发);头注/boot 注释更新(权威源 = Web 设置页;鉴权 DB 化陈述句) | 决策③`os server` 零业务参数 |
| `internal/cli/endpoint.go`(改) | `--token` help 去 env 名:`needs master key: run /setup to create <db>.key` | §3.3 #3 |
| `internal/cli/intake.go`(改) | 注释改离线冒烟姿势:公司 `issue_source=fixture + issue_fixture_path`(Web/API 配),env 仅测试 seam | §3.3 #4 |
| `internal/github/github.go` `fixture.go`(改) | 注释清理:构造语义(NewClient(token) 收参 / LoadFixture(path)),去 env 字样 | §3.3 #5 |
| `internal/service/engine.go` `task.go` `planner.go` `intake.go`(改) | 既有「env 仅测试 seam」注释体例统一「(9.4:产品恒关,DB 权威)」;engScripted/engScriptedPlan/engScriptedTriage 补「9.4:scripted 唯一 env 入口 = engineScripted 的 OS_ENGINE_MODE(envSeam 门,产品恒关)→ 本桩产品不可达」注 | §3.3 #7 / 3.4;OS_SCRIPT_* 不门控(测试桩产品不可达,env 空有确定默认) |
| `internal/cli/serverconfig_test.go`(改) | D 用例适配新签名:`TestD1ServerConfigFromApp`(零值行 = 内置默认 + DefaultAppSetting 行 09:00 保留 + DB 行值生效 + digest 归一 map)/`TestD2ServerConfigFromAppBounds`(越界仅非零:port 65536/-1、poll -1、interval -1 → error;port 0/1/65535、poll 1、interval 1 → ok) | §五 D(见修订 1) |
| `internal/service/seam_test.go`(新) | `withSeamOff(t)` helper(SetEnvSeam(false) + Cleanup 复位 true)+ **F1** `TestF1EngineScriptedIgnoresEnvSeamOff` / **F2** `TestF2AgentCLIIgnoresEnvSeamOff` / **F3** `TestF3IssueSourceForIgnoresEnvSeamOff` / **F4** `TestF4AgentCLIFromEnvIgnoresEnvSeamOff` + `TestF4AgentCLIFromEnvSeamOnUnchanged`(seam-on 对照直测) | §五 F1-F4 |
| `internal/endpoint/seal_test.go`(改) | 追加 **F5** `TestF5EnvSeamOffIgnoresEndpointKeyEnv`:seam 关 + OS_ENDPOINT_KEY 已设 + 无 master → SealToken/OpenToken 报 master key not loaded 且不含 env 名 | §五 F5 |
| `internal/github/fixture.go`(改,排版) | gofmt(上表语义改内 2 行注释对齐) | 纪律:改单内文件才 gofmt |
| `docs/phase9/design/cli-readonly.md`(新) | 本阶段实施契约(§一~§七 全) | 定稿门登记 |
| `docs/phase9/stages/4.md`(本文档)+ `design/README.md` + `docs/进度总表.md` | 收口凭证 | |

依赖:全 stdlib + 既有;零迁移;无 go.mod 变更;**零 Web diff**(无 assets/页面改动 → 不跑 tsc/vite)。

## 验证

- gofmt 本次改动干净(seam_test、github.go 排版修正后清零;`internal/notify/*` pre-existing 不在改单不碰);`go vet ./...`、`go build ./...` 干净。
- `go test -count=1 ./...` 全绿;**600+ 既有 seam-on 测试零 body 改动**,经两个新增 TestMain 自动开 seam 原样通过(9.3 A1-A3、delegate/planner/tier/driver OS_ENGINE_MODE/OS_SCRIPT_*/OS_AGENT_CLI、settings_test/delegate_test OS_ENDPOINT_KEY 全绿)。
- `go test -count=1 -race ./internal/service/ ./internal/endpoint/ ./internal/cli/ ./internal/server/` 全绿。
- **F/D 用例对照契约 §五**(全部 PASS):
  - **F1** `TestF1EngineScriptedIgnoresEnvSeamOff`:seam 关 + OS_ENGINE_MODE=scripted 污染 → env 忽略:无行 → live(false);company scripted → true;company live 压过 global scripted(+env scripted)→ false;别家继承 global scripted → true;DB 错 → false。
  - **F2** `TestF2AgentCLIIgnoresEnvSeamOff`:env codex 污染 → 无行 claude;company claude 压过 global codex(claude);无 company 覆盖继承 global codex。
  - **F3** `TestF3IssueSourceForIgnoresEnvSeamOff`:env fixture(+有效路径)污染 → 忽略:缺省 github 无 secret → fail-closed `github_token`(非 fixture 成功,判据);company fixture+path → `*github.Fixture`。
  - **F4** `TestF4AgentCLIFromEnvIgnoresEnvSeamOff`/`TestF4AgentCLIFromEnvSeamOnUnchanged`:seam 关 → claude;seam 开(codex env)→ codex(对照,双态并存证明门控成立)。
  - **F5** `TestF5EnvSeamOffIgnoresEndpointKeyEnv`:seam 关 + OS_ENDPOINT_KEY 已设 + 无 master → SealToken/OpenToken 报 `master key not loaded`,错误不含 `OS_ENDPOINT_KEY` 字样。
  - **D1/D2** `TestD1ServerConfigFromApp`/`TestD2ServerConfigFromAppBounds`(见修订 1 语义)。
- **二进制冒烟(实跑 `/tmp/phase94_smoke.sh`,G1 8 项 + G2 2 项全 PASS)**:
  - **G1 污染 env 零参数回归**:scratch db;导出 `OS_ENGINE_MODE=scripted / OS_AGENT_CLI=codex / OS_ISSUE_SOURCE=fixture(无效路径)` → `os init` → **零参数 `os server` 按 DB 权威起 :8787**(污染 env 无 crash、不读无效 fixture)→ `/api/v1/setup`(免 bearer)建 console + master_key + `<db>.key` 0600 → 建公司 + PUT company settings(agent claude/live)→ **GET /settings 断言 default live / agent claude(DB 权威,env 污染无效)** → 9.3 regression 子集:PUT /settings partial(queue_interval_sec=13 生效)、company secret github_token set + 掩码 list、rotate-master-key(双表 re-key + holder 换新后 authed 仍 200)→ **重启(仍污染 env):key 从 `<db>.key` 装载、authed GET 仍可用** —— 证明产品密钥源 = DB/文件,非 env。
  - **G2 CLI 只读/写面**:读命令族(`company list/show`、`task list`、`approval list`、`endpoint list --company`、`audit list`、`tool list`、`overview --company`)零 env 零参数逐条跑通;**领域写命令仍可用**(`company create` → `task create` → `approval/audit/overview` 可见)—— 决策②边界实证(Web 已收编配置面无任何 CLI/env 写渠道,§二 2.3 静态核实)。

## 实施期修订(相对冻结契约)

1. **D 用例 zero-fill vs bounds 语义裁定**:契约 §五 D2 行文「port 0 → error」与 §三 3.2 `serverConfigFromApp` 代码(0 值先零填内置默认、bounds 检查在零填后)冲突 → **以 §三 3.2 代码为准**:0 值行 = 零填默认不越界;bounds 错误只来自非零越界行值(port 65536/-1、poll -1、interval -1)。D1/D2 用例注释写明该语义(`os server` app 实为 `svc.AppSetting()`:缺行已回 DefaultAppSetting,零填为防御冗余)。
2. **G1 冒烟断言层级**:契约明示「seam-off 行为权威在 F1-F5,binary 只做污染 env 回归」→ G1 按此收窄(不重复语义断言),但补一处文件级证明:rotate 后**重启**仍污染 env、authed 调用可用 → key 来自 `<db>.key`(非 env 兜底)。
3. **F4 补 seam-on 对照**:契约 §五 只列 seam-off;补 `TestF4AgentCLIFromEnvSeamOnUnchanged` 同函数 seam-on 直测,与 seam-off 双态并存,证明门控成立而非环境失效。
4. **gofmt 波及**:seam_test(表列对齐)与 github.go(注释改写后排版)2 文件;均在改单内(纪律允许),零语义。

## 冻结边界(留给后续子阶段)

- **Phase 9 完结**(9.1-9.4 全部收口;配置唯一写入口 = Web、CLI 只读/排障、产品零 env 配置、server 零业务参数、机密主密钥治理闭环)。
- 后续候选(方向既定不做或待立项):多管理员/RBAC、公司级自建 repo/webhook/更多机密维度 → 方向 config-governance 决策范围外;声明式流水线 declarative-pipelines 草案 → Phase 10 候选(未立项)。
- 真实飞书/Claude Code/GitHub/codex live 验收(本阶段与 9.1-9.3 一致:离线 fixture + file:// test-double)待用户。
