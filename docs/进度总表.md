# 进度总表

> 全局里程碑跟踪,贯穿所有阶段。每完成一个交付物在此登记,作为续接的唯一依据(不靠记忆)。

## 阶段总览

| 阶段 | 定位 | 状态 | 交付物 |
|---|---|---|---|
| Phase 0 | Company Kernel | ✅ | CLI 可创建/读取核心实体 + SQLite 存储 + Audit 记录 |
| Phase 1 | Execution Kernel | ✅ | Task Queue / Workflow 引擎 / Tool / Workspace / Docker Sandbox / Approval / Execution / Permission 校验 |
| Phase 2 | Engineering Capability | ✅ | 首个业务能力域(Architect/Coding/QA/Review Agent) |
| Phase 3 | Research / Product | ✅ | 跨 Capability Workflow |
| Phase 4 | Business Capabilities | ✅ | Marketing / Sales / Customer / Operations / Finance |
| Phase 5 | One-Person Company OS | ✅ | 完整形态 |
| Phase 6 | 研发部(Engineering)能力就位 | ✅ | 6.1 模型池落库 + Provider 真实现(2026-09-02);6.2 回合驱动 + 熔断(2026-09-02);6.3 服务化通道 + 研发 Intake(2026-09-02);6.4 Planner 拆解 + 两路接活(2026-09-02);6.5 运营视图 + 触达(2026-09-02,overview RD 聚合 + 飞书通知),见 [design](phase6/design/rd-capability.md)。本地离线冒烟全通过;真实 飞书/Claude/GitHub live 验收待用户 |
| Phase 7 | 运营控制台(JSON API + Web SPA) | 🔶 | 7.1 JSON API 契约 + `/api/v1` 读/写端点(2026-09-02,overview/组织/任务/审批/决策/记忆/审计/端点池/通道 B;snake_case tag + bearer 认证),见 [web-console](phase7/design/web-console.md) 与 [stage 1](phase7/stages/1.md)。离线冒烟 24/24 通过(httptest 5 例)。7.2 控制台 UI 设计定稿 + 可交互原型(2026-09-02,actor 贯通 `human:console` + `os server --queue-work` 队列自驱),见 [console-ui](phase7/design/console-ui.md) 与 [stage 2](phase7/stages/2.md)。7.1/7.2 子阶段冻结。7.3 Web 控制台实现(2026-09-03,React 18+TS+AntD v5 SPA,8 视图全接真 `/api/v1` 非 mock,`go:embed` 单二进制同源托管 + `web/` Vite 工程 + `make ui` 嵌入管线),见 [stage 3](phase7/stages/3.md)。剩余:分页/CORS 按需 + 真实 飞书/Claude/GitHub live 验收待用户 |
| Phase 8 | 模型运行时:多厂商 tool-use + 角色分层 | 🔶 | 方向设计定稿(2026-09-04),经 **修订 A**(自建 OpenAI 兼容网关单 key 聚合多厂商、function calling 透传 → Provider 收敛为单一 Chat Completions 适配)与 **修订 B**(2026-09-04 定稿:阶段执行改**委派集成 agent CLI** —— claude Code(→codex 等)在任务 git workspace 真干、OS 用 git 捕获真实 diff 作产出;网关模型按档只承担判读/拆解/评审文本角色;原 8.2 原生 tool-call 闭环实施后不采用、整体回滚,8.3/8.5 取消并入),见 [design](phase8/design/model-runtime.md)。**8.1 Provider 抽象升级 ✅(2026-09-04)**:OpenAI 兼容网关 messages+tools 契约(`Chatter`)+ 网关 HTTP 实现 + ChatStub + claude CLI 标废弃保留,见 [provider-upgrade](phase8/design/provider-upgrade.md) 与 [stage 1](phase8/stages/1.md)。子阶段重排(修订 B):8.2 执行委派首个闭环(writer)→8.3 agent CLI 工具族+委派边界+判读结构化→8.4 档位分层强制。**8.2 ✅(2026-09-04)**:writer(live)委派 claude Code 在任务 git workspace 自主读/改/跑,OS 下发有界简报+约束(workspace/超时/预算),委派结束 **Go 用 git 捕获真实 diff** 作阶段产出 + 逐次委派审计 + 认领起点前置(非 git 硬错;脏起点须先有本任务委派记录才放行,防自锁);判读生产路径(planner/triage/test/review)**切网关**:`modelCall` 由 claude CLI 文本改 **OpenAI 兼容网关单轮 text Chat**(proto=openai 门,`finish_reason=length` 显式截断错),见 [agent-delegation-loop](phase8/design/agent-delegation-loop.md)(含 §八 实施修订)与 [stage 2](phase8/stages/2.md)。**8.3 ✅(2026-09-06)**:判读结构化(planner/test/review/triage 信号 = **JSON 主契约** + 旧标记兜底,统一解码收 `judge.go`;test 失败摘要 / review reason 捕获并 **A3 回喂下一 writer**)+ agent CLI 工具族注册选型(OS_AGENT_CLI 默认 claude,codex 注册槽位 + LookPath 门)+ 委派边界 / 共享工作树仲裁(**OS 逐次委派自动 commit**:baseline ref `refs/os/tasks/<id>` 钉任务起点、capture = 净 diff、委派后工作树归 clean → split≤8 子任务共享 repo 串行自锁根治;`.claude/` `.codex/` 残留经 `.git/info/exclude` 排除不入 diff/不脏 clean 门),见 [delegation-and-judging](phase8/design/delegation-and-judging.md)(含 §七 实施修订)与 [stage 3](phase8/stages/3.md);8.4 档位分层强制实施 ✅(2026-09-06,见下),契约定稿见 [tier-enforcement](phase8/design/tier-enforcement.md) |

| Phase 9 | 配置治理中心(Web 唯一配置入口 + company 隔离 + 机密治理) | 🔶 | 方向定稿(2026-09-06,定稿门 8 项:company 隔离 / 主密钥 Web 首启生成-显示-落盘 0600 / enc:v1 re-key / 全量 Web 化 / CLI 只读·Web 唯一写入口 / go test env seam / server 零参数·flag deprecated),见 [design](phase9/design/config-governance.md) 与 [设计索引](phase9/design/README.md)。**9.1 配置存储地基 ✅(2026-09-06)**:三表迁移 0012 + 领域/密钥/seal + endpoint seal With 注入 + repo/service 能力 + 单测全绿,见 [stage 1](phase9/stages/1.md);纯地基不接运行时/Web。**9.2 控制台访问治理 ✅(2026-09-06)**:endpoint 包级主密钥 holder(`UseMasterKey`)+ `/setup` 首启(Web 生成主密钥落 `<db>.key` 0600 + 可选旧 OS_ENDPOINT_KEY 一次性 re-key + console 令牌哈希化)+ `/api/v1` 鉴权 DB 化(空哈希开放/非空 Bearer constant-time)+ `PUT /settings/console-token` 轮换 + CLI root 主密钥注入 + 最小 SPA(/setup 向导显示主密钥一次 + 壳内「设置」页控制台访问卡);零迁移;tsc/build 绿 + 全量 go test/-race 绿 + **二进制冒烟实跑通过**(open→setup→0600 key→401/200→重启持久化→无 env CLI seal 成功且明文未落库),见 [console-access](phase9/design/console-access.md) 与 [stage 2](phase9/stages/2.md)。**9.3 运行时旋钮 Web 化 + 全量设置页 + 通知按公司 ✅(2026-09-07)**:运行时旋钮 DB 化(engineScripted/agentCLI/issueSourceFor 走 company→global→默认解析,env 仅测试 seam)+ 通知/摘要按公司 fan-out(公司机密 feishu_webhook/feishu_secret,删 env,独立日报)+ settings 主密钥 holder + `os server` 零参数(DB app_setting 权威、flag deprecated)+ 全量设置 API(GET/PUT /settings 保留 console_token_hash 红线 guard、company 覆盖/机密白名单掩码、POST rotate-master-key 双表 re-key 一次返回)+ 全量设置页 6 卡;零迁移;A1-E4 用例全绿 + tsc/build + **二进制冒烟 33/33 通过**(零参数重启 DB 起 port、PUT 后令牌仍 200、secrets 无明文、rotate 新 key 落盘 0600、digest-now 每公司独立日报),见 [runtime-knobs-web](phase9/design/runtime-knobs-web.md) 与 [stage 3](phase9/stages/3.md)。**9.4 CLI 只读收口 ✅(2026-09-07)**:env seam **反向门控**(service/endpoint 包级 `envSeam` 默认 false = 产品恒不读 OS_* 配置 env,纯 DB company→global→默认解析;仅 go test 经 TestMain 开,600+ 用例零 body 改动)+ 两个 TestMain + `os server` **5 业务 flag 全删**(--port/--poll/--digest/--queue-work/--queue-interval;只留 --db/--config + --digest-now 一次性触发;`serverConfigFromApp` 纯 app_setting → config,bounds 只来自非零越界行值)+ 残留文案清理(help/注释去 env 当配置字样,OS_SCRIPT_* 补「产品不可达」注不门控)+ **领域写命令全保留**(company/task/approval/…,非 Phase 9 收编面);零 Web diff;零迁移;F1-F5/D1-D2 用例全绿 + 二进制冒烟 **G1/G2 全 PASS**(污染 env scripted/codex/fixture 零参数 boot 按 DB 权威 + GET /settings live/claude + 9.3 regression 子集 + rotate 后重启 key 从 <db>.key 装载;CLI 读族零 env 跑通 + company create→task create→approval 可见),见 [cli-readonly](phase9/design/cli-readonly.md) 与 [stage 4](phase9/stages/4.md)。**Phase 9 完结**;**Phase 10 已立项**(方向定稿 2026-09-07,见下;10.1 实施契约待起草) |
| Phase 10 | 声明式流水线 + 项目目录容器(页面写意图落库即生效,加流水线不改代码;项目=目录容器,委派只写目录内) | 🔶 | 方向定稿(2026-09-07,方向定稿门 4 项:D7 案 A 项目 = company 下新一等实体,channel B repo 收敛为其 code 源绑定 / D6 首期锁三类形态 bugfix·开发·运维巡检 / D3 例行模板化计划先行、高智合成 10.4 / knowledge 口径 = 文件为准 + Memory 索引),见 [design](phase10/design/declarative-pipelines.md) 与 [设计索引](phase10/design/README.md)。**10.1 项目实体 + 声明式流水线落库 ✅(2026-09-08)**:project(company 下目录容器,root=整项目 git 仓库;已 git 直接用 / 空或不存在 OS mkdir+git init / 非空非 git → 400)/ pipeline(绑 project,声明式意图落库)+ task.project_id(只增不改,迁移 0013);run 复用 engineering driver(**异步建单**,意图=request 覆盖否则 description,**同 project 活跃 run 串行 409**,8.4 判读档落槽);受控 DELETE(project 无活跃可删,历史任务断引用保留,磁盘目录绝不碰);API/CLI + **Web 作者面同步**(项目/流水线两页两 Modal;pipeline 作者面 Web 唯一,CLI 只 list/show/run/delete);P1-P13 + HTTP + G1-G11 冒烟全绿,见 [project-pipeline-foundation](phase10/design/project-pipeline-foundation.md) 与 [设计索引](phase10/design/README.md) 与 [stage 1](phase10/stages/1.md);**10.2 schedule 调度 + ops_patrol 巡检真形态 ✅(2026-09-08)**:新包 internal/schedule(cron 五段数字子集 Parse/Next)+ 两迁移(task.pipeline_id 0014 / app_setting.schedule_poll_sec 0015 只增)+ runPatrol 巡检驱动(scripted/live 同路径:报告 `<root>/patrol/<taskID>.md` 真落盘 + OS 逐委派提交只 add 报告文件不吞脏改动 + 读回判读 verdict + 处置链:fix+high 链拉同项目 bugfix actor=system:patrol_chain 变更仍过审批;其余 notify+manual 留人)+ runClaimed 分流 + server ScheduleLoop/scheduleOnce(独立总开关 schedule_poll_sec 0=关;到点只建单 actor=system:schedule,queue-work 消费)+ PUT /pipelines/{id} 改调度 + GET /projects/{id}/patrol/{taskID} 报告受控读端点(路径服务端推导无穿越)+ Web 同步(cron 建/改 + runs 形态·裁决列 + 报告抽屉 + 设置行 schedule_poll_sec 回显);S1-S11 + Srv-1..4 + E1 扩全绿 + tsc/vite + 二进制冒烟(schedule_poll_sec 落/回显;ops_patrol 建/cron 校验/读端点 404),见 [ops-patrol-schedule](phase10/design/ops-patrol-schedule.md) 与 [stage 2](phase10/stages/2.md);**10.3 run 计划账本通用化 ✅(2026-09-08)**:迁移 0016 task_plan/task_plan_phase + internal/plan 包 + 账本助手(plan.go)+ patrol upfront 预铺(建单即铺 5 阶段,先审后干)/engineering grow 执行记账(回合边界 append 幂等)+ 计划审批 = 增强既有门(waiting_approval 审批页随单展示预铺计划)+ patrol 判读前 OS 机械卫生预检(D4:pre/post porcelain 对账,残留→accept fail 不经判读)+ GET /tasks/{id}/plan 只读端点(非流水线/历史 run → null)+ Web 三处观察面同步(TaskDrawer 计划段/ProjectDetail runs 行「计划」/审批 DecideModal 随单展示);T1-T6 + Tsrv-1 用例全绿(既有 600+ 零 body 改)+ 全量验证 + 二进制冒烟(run→200 建单 8.4 落槽 → plan 读回 5 条 pending),见 [phase-plan-contract](phase10/design/phase-plan-contract.md) 与 [stage 3](phase10/stages/3.md);**10.4 高智运行时合成 + 预算**:frontier 对意图合成计划(承 8.4 档位)+ 例行模板化(D3)+ 阶段/全局预算 |

## 子阶段明细

| 日期 | 子阶段 | 交付物 | 文档 | 备注 |
|---|---|---|---|---|
| 2026-09-01 | Phase 0 实现+验证 | CLI 8 组命令 + SQLite 存储 + Audit 自动记录 | docs/phase0/stages/0.md | smoke test 全通过，阶段冻结 |
| 2026-09-01 | Phase 1.1 Execution Core | Task Queue + 状态机 + Execution + Worker + Runtime 接口 | docs/phase1/stages/1.md | smoke test 全通过，子阶段冻结 |
| 2026-09-01 | Phase 1.2 Tool+Workspace+Sandbox | Tool 接口+Registry + Docker Sandbox + 独立 Workspace + 调用前权限校验(默认拒绝) | docs/phase1/stages/2.md | smoke test 全通过，子阶段冻结 |
| 2026-09-01 | Phase 1.3 Workflow Engine | definition 解析 + 按序生成/执行节点 Task + 失败中断 + os workflow run | docs/phase1/stages/3.md | smoke test 全通过，子阶段冻结 |
| 2026-09-01 | Phase 1.4 Approval+Permission 校验 | 审批触发(risk=high OR approval policy) + waiting_approval + approve/reject/changes + 权限校验 | docs/phase1/stages/4.md | smoke test 全通过，Phase 1 收口 ✅ |
| 2026-09-01 | Phase 2.1 Engineering Tools | task.tool_name(迁移 0004) + git/file-read/file-write Tool(宿主 workspace + 边界强制) + os task create --tool | docs/phase2/stages/2.md | smoke test 全通过，子阶段冻结 |
| 2026-09-01 | Phase 2.2 Engineering Workflow | 8 阶段定义(issue→delivery) + 审批门暂停/续跑/中断 + 节点失败中断 + workflow run 进度输出 | docs/phase2/stages/3.md | smoke test 全通过，子阶段冻结 |
| 2026-09-01 | Phase 2.3 4 Agent + Provider 接入 | Model Provider 抽象+config(provider list) + agent --model + aifix Tool(mock) | docs/phase2/stages/4.md | smoke test 全通过，Phase 2 收口 ✅ |
| 2026-09-01 | Phase 3.1 Research/Product Capability 就位 | research+product Capability(各 3 Agent,带 model)+ 最小权限策略 + 默认拒绝验证 | docs/phase3/stages/1.md | smoke test 全通过，子阶段冻结 |
| 2026-09-01 | Phase 3.2 跨 Capability Workflow 引擎 | WorkflowNode.capability + (capability, role) Agent 解析 + Task 落 capability_id + 向后兼容 | docs/phase3/stages/2.md | smoke test 全通过，子阶段冻结 |
| 2026-09-01 | Phase 3.3 端到端跨 Capability 流程 | 产品开发 8 节点(Idea→Research→Product→Engineering→QA→Approval→Delivery)跨 3 Capability 一键运行 + 审批门 approve/reject | docs/phase3/stages/3.md | smoke test 全通过，Phase 3 收口 ✅ |
| 2026-09-01 | Phase 4.1 Marketing/Sales Capability 就位 | marketing(3 Agent)+ sales(3 Agent)Capability + 最小权限策略 + 默认拒绝验证 | docs/phase4/stages/1.md | smoke test 全通过，子阶段冻结 |
| 2026-09-01 | Phase 4.2 Customer/Operations/Finance Capability 就位 | customer/operations/finance(各 2 Agent)Capability,5 个 Business Capability 齐 + 最小权限 | docs/phase4/stages/2.md | smoke test 全通过，子阶段冻结 |
| 2026-09-01 | Phase 4.3 端到端业务流程 | 用户反馈→Marketing→Sales→Customer→Operations→Finance→Approval→Delivery 跨 5 Business Capability 一键运行 + 审批门 approve/reject | docs/phase4/stages/3.md | smoke test 全通过，Phase 4 收口 ✅ |
| 2026-09-01 | Phase 5.1 Memory 落库 + 搜索 | migration 0005(memory+FTS5 trigram)+ os memory add/list/search + source 血缘 + audit | docs/phase5/stages/1.md | smoke test 全通过，子阶段冻结 |
| 2026-09-01 | Phase 5.2 Decision 落库 + 自动记录 | migration 0006(decision)+ os decision add/list/show + approval 结果自动落 decision(治理链闭环)+ audit | docs/phase5/stages/2.md | smoke test 全通过，子阶段冻结 |
| 2026-09-01 | Phase 5.3 全景运营视图 | os overview 单命令聚合 7 区块(company/capabilities×agents/workflow 状态/待审批/decision/task/memory 高亮) | docs/phase5/stages/3.md | smoke test 全通过，子阶段冻结 |
| 2026-09-01 | Phase 5.4 完整形态流程 | 新品发布 9 节点跨 6 Capability 家族(research/product/engineering/marketing/sales/operations)一键运行 + 审批门 approve/reject + decision 自动落库 + memory 沉淀 | docs/phase5/stages/4.md | smoke test 全通过，Phase 5 收口 ✅ |
| 2026-09-02 | Phase 6.1 模型池落库 + Provider 真实现 | migration 0007(endpoint)+ AES-GCM token 加密落库(enc:v1:,OS_ENDPOINT_KEY,fail-closed)+ ClaudeCLI Provider(claude -p)+ os endpoint add/list/show/models/select + 厂商自动识别 + models_cache | docs/phase6/stages/1.md | smoke test 全通过，子阶段冻结;真实端点/key 验收待用户 |
| 2026-09-02 | Phase 6.2 回合驱动 + 熔断 | migration 0008(task 回合扩展)+ Engineering Driver(写→测→审回合机,conflict+1 返工,达 3 熔断→waiting_approval→approve 续跑 decision 自动落库)+ 模型调用分界(OS_ENGINE_MODE live/scripted)+ engineering 家族分发(0-5 语义不变)+ task create/show 接线 | docs/phase6/stages/2.md | scripted 冒烟 5 场景全通过，子阶段冻结;live 真实端点/claude 验收待用户;真实代码落盘/测试执行属 6.3 |
| 2026-09-02 | Phase 6.3 服务化通道 + 研发 Intake(通道 B) | migration 0009(repos + issue_sync 账本)+ GitHub issue 源(REST client/fixture)+ 研发 Intake(每 issue 分诊四处置,direct_work→create engineering task 入队)+ os repo add/list + os intake sync + os server(chi /healthz + /api/webhook/github + 轮询兜底)+ modelCall 抽取 + chi v5 依赖 | docs/phase6/stages/3.md | 离线冒烟 5 场景全通过(direct/skip/去重/webhook 实时/轮询/live fail-closed)，子阶段冻结;真实 GitHub token + 公网 webhook 验收待用户;planner 拆解/通道 A 接活属 6.4 |
| 2026-09-02 | Phase 6.4 Planner 拆解 + 两路接活 | Driver planner 序曲(顶层工程请求认领即拆解:direct→回合机 / split≤8 子任务挂父同步驱动聚合 / ask>8 走 approval 无 bypass)+ ListTasksByParent + planner 模型分界(live JSON/scripted OS_SCRIPT_PLAN)+ 通道 A(workflow engineering 节点整包接活,引擎语义零改动)+ 通道 B(merge→umbrella task 入队,6.3 待办收口)+ planner 单测 | docs/phase6/stages/4.md | 冒烟 A-F 全通过(split≤8 聚合/两节点续跑/merge 拆解/ask>8 审批/6.2 direct+熔断回归/续跑幂等/live fail-closed)，子阶段冻结;真实 Claude planner 端点验收待用户;飞书通知/运营视图属 6.5 |
| 2026-09-02 | Phase 6.5 运营视图 + 触达 | internal/notify 飞书 webhook(https/file:// 双 sink + secret 签名)+ requestApproval 事件点即时通知(熔断/待审批)+ overview RD 聚合块(pending/熔断/待审批/子任务/issue_sync 账本处置分布)+ 昨日摘要(service 24h 窗口 + server --digest 单协程定时 09:00 + --digest-now)+ CLI 接线。无新迁移/sqlc/依赖 | docs/phase6/stages/5.md | 冒烟 S1-S6 全通过(S1 RD 块/S2 熔断即时告警/S3 待审批即时/S4 昨日摘要/S5 https 单测/S6 fail-open 回归)，子阶段冻结;真实飞书 webhook + 6.1-6.4 live 端点验收待用户;后续可引 robfig/cron 正式调度 |
| 2026-09-02 | Phase 7.1 JSON API 契约 + `/api/v1` 端点 | 12 域/视图模型补 json snake_case tag + 信封 `{ok,data}`/`{ok:false,error:{code,message}}` + `sql.ErrNoRows→404` + bearer 中间件(`OS_API_TOKEN`,constant-time,未设=开放)+ internal/server/api.go 27 端点(companies/overview(含 RD 块)/capabilities/agents/workflows/tasks+executions/approvals+decision/decisions/memories+search/audit/endpoints+select+models/repos+intake sync)+ server.go `/api/v1` 路由组 + CLI server 读 token。无新迁移/sqlc/依赖(chi 已有) | docs/phase7/stages/1.md | httptest 5 例 + 离线 curl 冒烟 PASS=24 FAIL=0(overview RD 熔断块/审批 approve 回队/记忆搜索/repo+intake 账本/404+400/OS_API_TOKEN 401+200)，子阶段冻结;真实飞书/Claude/GitHub live 验收待用户(承 6.x);UI 属 7.2 |
| 2026-09-02 | Phase 7.2 控制台 UI 设计 + 契约缺口回填 | service 写方法全套 `*As` 变体 + `/api/v1` 写操作审计 actor=`human:console`(CLI 保持 `human:cli`)+ Server.QueueLoop(`--queue-work`/`--queue-interval`,每 tick RecoverLeasedTasks + LeaseAndExecute("server") 排空上限 16,默认关)+ CLI server 接线 + 可交互高保真原型 [console-ui-prototype.html](phase7/design/console-ui-prototype.html)(8 视图/双主题/审批三态决策与 service 同语义) + 设计定稿 [console-ui.md](phase7/design/console-ui.md)(IA/令牌/交互规格/端点映射) | docs/phase7/stages/2.md | go build/test 全绿;httptest 新增 actor 贯通 + 队列消费端到端(queue_test.go,file-write 落盘断言)+ 真实 `os server --queue-work` 冒烟(`create human:console → runtime:server → completed`),子阶段冻结;7.3 React SPA(未来)+ live 端点验收待用户 |
| 2026-09-03 | Phase 7.3 Web 控制台实现 | 仓库根 `web/`(React 18 + TS + AntD v5 + Vite + react-router v6,三态主题/轮询/信封 client/boot 门)+ 8 页(总览/审批/决策/任务/研发仓库/知识库/审计/模型端点池)全量接真 `/api/v1`(契约逐字对齐 Go json tag;overview null-切片兜底;models_cache 防御 parse;approval 标题懒取;审批三态 `human:console` 决策)+ `internal/console`(go:embed ui,SPA fallback,/api·/healthz·带扩展名缺失→404,非 GET→405)+ server.go `/*` 挂载(在 /api/v1 后)+ `Makefile`(ui/build/dev/test/clean)+ `.gitignore` + 原型全屏右半空白修复(`.content` 去 max-width) | docs/phase7/stages/3.md | tsc `--noEmit` 干净 + `vite build` 产出 dist;`go build ./...` + `go test ./...` 全绿(含新增 console 挂载 httptest);离线 seed 起 `os server` 冒烟:SPA index + client 路由 fallback 200 + hashed asset 200 + `/api/v1/companies` 空[]→POST 建公司→overview(RD null)信封正常;/api/未知 404。7.1/7.2/7.3 子阶段冻结;剩余分页/CORS 按需 + live 端点验收待用户 |
| 2026-09-04 | Phase 8 立项(修订 A)+ 8.1 Provider 抽象升级实施 ✅ | 方向 [model-runtime](phase8/design/model-runtime.md) 定稿 + 修订 A(自建 OpenAI 兼容网关)后立项;8.1 落地 `internal/provider` 纯新增:OpenAI Chat Completions 方言 `messages.go`(类型 + 构造助手 + arguments 互转)+ `chatter.go`(`Chatter` 接口)+ `openai.go`(网关 HTTP:`{base}/v1/chat/completions`,Bearer 单 key、function calling 透传)+ `chatstub.go`(确定性档)+ `ClaudeCLI` 头注标废弃(代码零改动);httptest 假网关 5 用例,见 [provider-upgrade](phase8/design/provider-upgrade.md) 与 [stage 1](phase8/stages/1.md) | `go build ./...` + `go test ./...` 全绿(provider 新增测试全过,0-7 零改动,gofmt/vet 干净);无迁移/sqlc/依赖,service/endpoint 零改动;真实网关 live 验收待用户(随 8.2) |
| 2026-09-04 | Phase 8 修订 B 方向修正定稿 + 8.2 原生 tool 闭环回滚 ✅ | 方向 [model-runtime](phase8/design/model-runtime.md) §十一 修订 B:阶段执行不再用 OS 自研原生 tool-call 编排,改**委派集成 agent CLI**(claude Code 首位 → codex 等)在任务 git workspace 自主真干,OS 下发简报 + 约束 + 逐次委派审计,委派结束 **Go 用 git 捕获真实 diff** 作产出;判读(planner/test/review)走网关分档 text Chat、信号结构化;模型/工具解耦(writer 委派 claude 可配 cheap,如 `ANTHROPIC_BASE_URL` → 网关 cheap 或 claude 自带模型)。原 8.2(原生 tool-call writer 闭环,dev `f761abc` 曾实施)经用户评审不采用、整体回滚至 `1e610c4`;原 8.3 取消、8.5 并入;子阶段重排 8.2(执行委派首个闭环 writer)→8.3(工具族+委派边界+判读结构化)→8.4(档位强制,保留) | [model-runtime](phase8/design/model-runtime.md) §十一(受影响段 §一/§二/§三/§四 D1·D2·D4/§六/§八/§十,定稿门 2026-09-04 用户确认收口) | 纯文档改动(design/README 已登记);代码层 8.2 原生闭环已回滚至 8.1 收口态(`go test ./...` 绿);下一子阶段 8.2 = 执行委派首个闭环(writer) |
| 2026-09-04 | Phase 8.2 执行委派首个闭环(writer)+ 判读回网关实施 ✅ | 实施契约 [agent-delegation-loop](phase8/design/agent-delegation-loop.md)(定稿后实施,含 §八 实施修订)落地:service `delegate.go` 新增(Delegator/claudeDelegator + git 助手 + delegateWriter/delegateBaseline/delegateEnv/delegateBrief)+ `engine.go` `modelCall` 切 OpenAI 兼容网关单轮 text Chat(proto=openai 门 + length 截断显式错)+ `engCall` live writer → 委派 + `driver.go` live 认领前置 + `service.go` delegator 注入 + `repository/audit.go` 增 `HasAuditAction` + claudecli 头注改口 + delegate_test/driver_test 新用例;归档 [stage 2](phase8/stages/2.md) | `go build ./...` + `go vet ./...` 干净 + `go test ./...` 全绿(0-7/8.1 零改动);新增 9 用例全过(git 助手三态/委派闭环跑通 writer→test→review 假网关+审计/判读 proto 门/截断/空 diff/scripted 回归/前置失败对照) | 无迁移/sqlc/go.mod;scripted 服务层确定性文本与 6.2 一致;真实 claude Code live 委派验收待用户;下一子阶段 8.3 = agent CLI 工具族 + 委派边界 + 判读结构化 |
| 2026-09-06 | Phase 8.3 判读结构化 + agent CLI 工具族 + 委派边界/共享工作树仲裁实施 ✅ | 实施契约 [delegation-and-judging](phase8/design/delegation-and-judging.md)(定稿门:单阶段整体交付 + OS 逐次委派自动 commit;含 §七 实施修订)落地:`judge.go` 新(A1 统一解码 + parseTest/parseReview/parseDisposition + 旧标记兜底)+ `engine.go` engScripted 判读切 JSON + engCallCtx.hint + `driver.go` 返工喂失败原因(A3)+ `intake.go`/`planner.go` 判读 JSON 收口 + `delegate.go` 族注册表(codexDelegator 槽位 + delegatorFor + DelegateSpec.Family)+ C1 助手(wsBaselineRef/excludeWorkspaceTools/ensureBaseline/captureNet/commitDelegation/delegateCommitMsg)+ delegateWriter 新流程(委派 → 净 diff → audit → **OS commit**)+ delegateBrief hint 段 + audit detail 带 family+ref + claudecli 头注指针;归档 [stage 3](phase8/stages/3.md) | `go build`/`go vet` 干净 + `go test -count=1 ./...` 全绿(0-7/8.1 零改动;8.2 测试仅按新契约适配)+ `-race` service 绿;新增 10 用例全过(A1/A2 judge 单测、A4 rework hint、B1 族选型、C1 净 diff+clean+commit、C2 共享 ws 两子任务串行、C3 kill-window 兜底、C4 .claude 排除) | 无迁移/sqlc/go.mod;scripted 分界与确定性保持;真实 claude Code live 委派(commit 收口 + 净 diff 判读闭环)/codex 真适配/cheap 配对验收待用户;下一子阶段 8.4 = 档位分层强制 |
| 2026-09-06 | Phase 8.4 档位分层强制实施 ✅ | 实施契约 [tier-enforcement](phase8/design/tier-enforcement.md)(定稿门 2026-09-06 三项:① test 分槽 = 新增 task.test_endpoint_id(0011);② writer 默认只从 cheap+可作 claude 后端(proto=anthropic)解析、无则留空;③ 判读槽默认档无 active 端点 → **建单即硬失败**;含 §七 实施修订)落地:**迁移 0010/0011 只增不改**(endpoint.tier `NOT NULL DEFAULT 'standard'` 存量回填 / task.test_endpoint_id REFERENCES endpoint)+ schema/queries/sqlc 重生成(diff 仅新增列 + UpdateEndpointTier)+ repo CreateEndpoint 空档归一 standard(INSERT 显式列绕 DEFAULT 补回)+ service `tier.go` 新(tier 常量 + isScriptedEngine live 门 + `applyEndpointDefaults` 三槽确定性解析 created_at/id 决胜:review=frontier+openai、test=standard+openai、writer=cheap+anthropic;判读槽硬失败带 add/select/显式 flag 指引)+ createTask 单钩子(engineering+live → 空槽默认落位 + create audit detail `reviewer=/test=/writer=<id>:<model>`)+ engEndpointFor test 分槽(test→reviewer→writer)+ plan_driver 子任务继承 test 槽 + SelectEndpointModel tier 参数(先校验再落库防半写)+ CLI select `--tier`/list TIER 列/show TIER 行(task create `--test-endpoint`)+ server create req/select body + console Endpoint.tier/端点池「档位」列 + select 弹窗 tier 可选项 + Task.test_endpoint_id;归档 [stage 4](phase8/stages/4.md) | `gofmt` 本次改动干净 + `go vet`/`go build` 干净 + `npx tsc --noEmit`(web)干净 + `go test -count=1 ./...` 全绿(**0-7/8.1 零改动**;8.2/8.3 仅按新契约适配——seedEndpoint 归一 standard 后多数零改动,纯 writer 无判读端点用例补 seedJudgeDefaults)+ `-race` service 绿;新增 tier_test.go C1-C6/B1/A2 与 server tier_api_test.go C8 全过 | 两迁移;无 go.mod 变更;scripted 分界与确定性保持;真实网关多档 live 验收(建单默认落档跑通 test=standard/review=frontier 真实判读 + cheap claude 委派配对)+ 运行时按档自动换 planner 端点 + 预算成本仪表盘留待后续 |

| 2026-09-06 | Phase 9 方向定稿(配置治理) | 方向 [config-governance](phase9/design/config-governance.md) 定稿:配置模型两层(global 默认 + company 覆盖)+ 机密 AES-GCM 落 secret 表(company 级,主密钥 Web 首启生成落 0600 文件)+ 11 env 旋钮逐行收编(§三全量清单)+ server 零参数 + CLI 只读/排障 + /setup 首启 + 9.1–9.4 子阶段划分;登记 [design README](phase9/design/README.md)(declarative-pipelines 草案不入索引) | 定稿门 8 项决策通过(AskUserQuestion);未写码;下一子阶段 9.1 实施契约 + 落地 |

| 2026-09-06 | Phase 9.1 配置存储地基实施 ✅ | 实施契约 [settings-foundation](phase9/design/settings-foundation.md) 落地:迁移 0012(只增不改,`app_setting` 全局单行 id='self' / `company_setting` 每公司覆盖行 NULL=继承 / `secret` company 机密 PRIMARY KEY(company_id,id) 密文 enc:v2:)+ schema/queries/sqlc 重生成(diff 仅新增三表 + settings 12 查询 + UpdateEndpointToken)+ `internal/settings` 新包(model 默认常量 + keyfile 0600 <db>.key 64 hex + SealSecret/OpenSecret AES-GCM)+ endpoint seal 抽 `SealTokenWith/OpenTokenWith`(env 语义不变)+ repo settings.go/SetEndpointToken + service settings.go(AppSetting 缺行→默认、CompanySetting ok=false、SetSecret/OpenSecret、`EngineModeFor` company→global→live、`RekeyEndpointTokens` 两遍零写坏)+ 单测 A1/A2/B1/B2/C1/C2/C3;归档 [stage 1](phase9/stages/1.md) | `gofmt` 本次改动干净 + `go vet`/`go build` 干净 + `go test -count=1 ./...` 全绿(0-7/8.x 既有测试零改动,seal env 语义不变)+ `-race` service 绿;新增 7 用例全过(A1 迁移表/默认列、A2 三表+secret 隔离+SetEndpointToken、B1 keyfile、B2 settings/endpoint seal 双、C1 生效解析、C2 re-key 成功+失败零写) | 一迁移;无 go.mod 变更;纯地基不接运行时/Web。实施修订:Upsert 改 repo Get→insert/update(sqlc SQLite 不解析 upsert RETURNING);db/queries/*.sql 保持纯 `-- name:`+SQL 无自由注释(sqlc edited-query 对长中文注释解析异常)。下一子阶段 9.2 控制台访问治理(/setup 主密钥首启 + console token 哈希) |
| 2026-09-06 | Phase 9.2 控制台访问治理实施 ✅ | 实施契约 [console-access](phase9/design/console-access.md) 落地(定稿门两项决策:含最小 SPA;endpoint 包级 holder):endpoint seal 包级主密钥 holder `UseMasterKey`/`CurrentKeySource`/`currentKey`(SealToken/OpenToken 改走 holder,env 仅测试 seam,enc:v1 语义不变)+ settings `HashConsoleToken` + service `ConsoleTokenHash`/`SetConsoleTokenAs`(audit `human:console`)/`SetConsoleToken` + server `apiAuth` **DB 化**(每请求读 console_token_hash:空=开放/非空 Bearer sha256hex constant-time;删 apiToken+SetAPIToken)+ `GET setup/status`/`POST setup`(未初始化才可;GenerateKey→可选旧 OS_ENDPOINT_KEY 一次性 re-key→SaveKey 0600→UseMasterKey→SetConsoleTokenAs;200 主密钥仅此一次;已初始化 409)+ `PUT settings/console-token` 轮换(旧即失效)+ CLI root 注入主密钥(`settings.KeyPath(path)`→LoadKey→UseMasterKey;server 接 masterKeyPath、去 env token)+ SPA(BootGate `/setup` 全屏向导显示主密钥一次 + 壳内「设置」页控制台访问卡:状态徽标 + 轮换表单);归档 [stage 2](phase9/stages/2.md) | `gofmt` 本次改动干净 + `go vet`/`go build` 干净 + `go test -count=1 ./...` 全绿(0-7/8.x 既有测试零改动,仅 TestAPIAuthToken 按新契约改写)+ `-race` server/service/settings/endpoint 绿;新增用例 A1/A2(+失败零写)/A3/SetupWithoutKeyPath + B1 holder + B2 hash/SetConsoleTokenAs + TestAPIAuthToken 改写全过;tsc `--noEmit` + `vite build` + `make ui` 重嵌 + go build 绿;**二进制冒烟实跑**:open→setup→`<db>.key` 0600→401/200→重启持久化→无 OS_ENDPOINT_KEY CLI seal 成功(`enc:v1:` 密文落库、明文零命中) | 零迁移;无 go.mod 变更。实施修订:A3 开头补未初始化 PUT 409 断言;Settings 轮换成功只 setToken 不 bump(initialized 不变);AuthModal/401 文案统一「控制台令牌」;apiAuth whitelist 须比全路径含 `/api/v1` 前缀。下一子阶段 9.3 旋钮 Web 化/全量设置页(engine/agent/issue/digest/server 参数 + 换主密钥 re-key) |
| 2026-09-07 | Phase 9.3 运行时旋钮 Web 化 + 全量设置页 + 通知按公司实施 ✅ | 实施契约 [runtime-knobs-web](phase9/design/runtime-knobs-web.md)(定稿门 2026-09-07 三项:① 摘要 = 按公司 fan-out 独立日报;② 通知删 env 只走公司机密;③ 引擎 Web 只收 live、scripted 仅作 CLI/env 测试 seam)落地:settings 主密钥 holder(`UseMasterKey`/`MasterKey`)+ service runtime.go(`engineScripted`/`agentCLI`/`issueSourceFor` company→global→默认解析,env 仅 seam;consumers engine/task/planner/delegate/intake 换装,删 isScriptedEngine/issueSource/intakeMode)+ notify.New(webhook,secret)(删 NewFromEnv;空 webhook nil)+ notifyCompany 按公司机密路由 + SendDailyDigest 按公司 fan-out + gatherDigestFor(approval 经本公司任务集过滤)+ service 删 notify 单例字段 + settings.go secret Current 族/DeleteSecretCurrent/RekeySecrets/**RekeyAll 双表零写**/SecretMeta 掩码 + patch.go + server settings_api.go 全量设置 API(GET/PUT /settings **保留 console_token_hash 红线 guard**、companies/{id}/settings GET/PUT/DELETE、companies/{id}/secrets 白名单掩码、POST settings/rotate-master-key 双表 re-key 一次返回;ErrSettingsBadRequest→400)+ setup/root 补 settings.UseMasterKey + cli serverconfig.go(`effectiveServerConfig` DB 权威 flag Changed 覆盖)+ server.go 零参数 + Web Settings.tsx 全量 6 卡(引擎只读 live / server 参数 / 公司覆盖+机密 / 换主密钥危险区)+ endpoints/types;归档 [stage 3](phase9/stages/3.md) | `gofmt` 本次改动干净 + `go vet`/`go build` 干净 + `go test -count=1 ./...` 全绿(0-7/8.x 既有测试仅 notify_test 按 New 改写;digest_test 纯 formatDigest 不动)+ `-race` service/server/settings/cli 绿;新增 A1/A2/A3/B1/B2/C1/C2(+零写)/C3/D1/D2/E1-E4(+guards)全过;tsc `--noEmit` + `vite build` + `make ui` 重嵌 + go build 绿(提交前恢复 index.html 占位);**二进制冒烟 33/33 通过**:零参数首启 :8787→/setup→0600 key→401/200→GET 默认→PUT(guard 令牌仍 200、scripted/空 patch 400)→重启零参数 **DB 权威起 :8790**(digest off 生效)→建公司+覆盖+机密(file:// 邮箱)→secrets 无明文→rotate(新 key 落盘 0600、旧 key 失效)→`--digest-now` 每公司独立日报 | 零迁移;无 go.mod 变更。实施修订:CreateApproval 只插 pending(已决审批须 UpdateApproval 补 decided_at,新 helper seedDecidedApproval);B2 窗口外审批不计;D 用例以 DefaultAppSetting 为真实输入、越界经显式 flag 可达;补 rotate guards(未初始化 400 / 无 key 路径 500);notify/service notify.go pre-existing 注释 gofmt 顺手。下一子阶段 9.4 CLI 写命令收口 / deprecated 清理(env 消费点删净、server flag 删除、os settings 只读/排障) |
| 2026-09-07 | Phase 9.4 CLI 只读收口实施 ✅ | 实施契约 [cli-readonly](phase9/design/cli-readonly.md)(定稿门 2026-09-07 三项:① env seam 门控 = 反向默认(测试开、产品恒关);② CLI 收口深度 = 收 env/flag + 文案清理,领域写命令全保留;③ server 业务 flag 删除)落地:service/endpoint 两包各加包级 `envSeam=false`+`SetEnvSeam`(seam.go 新 / seal.go 平行)+ runtime.go `engineScripted`/`agentCLI`/`issueSourceFor` 与 delegate.go `agentCLIFromEnv` env 分支收进门(seam 关 → 纯 DB company→global→默认;seam 开 → env-first 现行为不变)+ endpoint `envKey` 门控(seam 关 → 不读 OS_ENDPOINT_KEY,报 master key not loaded /run setup,文案不含 env 名)+ 两个 TestMain(service 开双 seam / endpoint 开单 seam;600+ 既有测试零 body 改)+ serverconfig `effectiveServerConfig`→`serverConfigFromApp(app)` 纯函数(零值行零填内置默认、bounds 只非零越界行值、digest 空/off 归一)+ `os server` 删 5 业务 flag(只留 --db/--config + --digest-now)+ 残留文案/help 清理(endpoint --token 去 env 名、intake/github 注释去 env 当配置、engine/task/planner/intake OS_SCRIPT_* 补「产品不可达」注不门控);归档 [stage 4](phase9/stages/4.md);**Phase 9 完结** | `gofmt` 本次改动干净(seam_test/github.go 排版;notify pre-existing 不碰)+ `go vet`/`go build` 干净 + `go test -count=1 ./...` 全绿(既有 seam-on 用例经 TestMain 零改原样绿)+ `-race` service/endpoint/cli/server 绿;新增 F1-F5 + D1/D2 全过(F1 engineScripted env 污染忽略走 DB company/global/live;F2 agentCLI env codex 忽略;F3 issueSourceFor env fixture 忽略 fail-closed github_token;F4 agentCLIFromEnv seam 关 claude + seam 开对照;F5 endpoint envKey seam 关报 master key not loaded 不含 env 名);**二进制冒烟 G1/G2 全 PASS**:污染 env(scripted/codex/fixture 无效路径)零参数 boot 按 DB 权威起 :8787 →/setup→0600 key→建公司+覆盖 agent claude/live→GET /settings live/claude(env 无效)→PUT /settings+公司机密+rotate(holder 换新 authed 仍 200)→**重启仍污染 env key 从 <db>.key 装载**;CLI 读族(company/task/approval/endpoint/audit/tool/overview)零 env 跑通 + company create→task create→approval/audit 可见 | 零迁移;无 go.mod 变更;**零 Web diff**(不跑 tsc/vite)。实施修订:D 用例 zero-fill vs bounds 以 §三 3.2 代码为准(0=零填默认不越界,越界仅非零,契约 §五 D2 行文冲突故裁定);G1 补 rotate 后重启证明密钥源=<db>.key;F4 补 seam-on 对照直测。真实 codex/live 验收与多管理员/RBAC 待后续 |
| 2026-09-07 | Phase 10 方向定稿(声明式流水线 + 项目目录容器) | 方向 [declarative-pipelines](phase10/design/declarative-pipelines.md) 定稿(由 phase9/design 同稿草案再定位升级:序号 9.x→10.x,立足点 8.3→8.4+Phase 9 完结态;补 D7 立项首分叉);登记 [design README](phase10/design/README.md) | 方向定稿门 4 项决策通过(AskUserQuestion,均按推荐):D7 案 A 项目=company 下新一等目录容器实体(company 保配置/机密/通知隔离,channel B repo 收敛为 project 的 code 源绑定);D6 首期锁三类形态 bugfix/开发/运维巡检(按项目参数化,第 4 种再泛化);D3 例行模板化计划先行、高智合成 10.4 接;knowledge 口径 = 文件为准 + Memory 索引(可重建)。未写码;用户指示「文档好之后就不做了,下次做」→ 下一工作 = 起草 10.1 实施契约(项目实体 + 声明式流水线落库) |
| 2026-09-08 | Phase 10.1 实施契约定稿(项目实体 + 声明式流水线落库 + Web 作者面) | 实施契约 [project-pipeline-foundation](phase10/design/project-pipeline-foundation.md) 定稿:run **复用 engineering driver**(不另起执行体;异步建单 + worker 驱动);目录 **layout A 整项目一个 git 仓库**(委派面=项目根,8.3 baseline/commit 语义直接复用);实体 projects/pipelines + task.project_id(迁移 0013,只增不改);登记 [design README](phase10/design/README.md) | 契约定稿门两项决策通过:① root 就绪 = 空目录/不存在 OS `mkdir+git init`,已存在须 git,非空非 git → 400;② 本期带**受控 DELETE**(project 仅无活跃 run 可删,历史任务先断 project 引用保审计,绝不碰磁盘)。未写码;下一工作 = 按契约实施(数据层 0013/sqlc/repository → service(RunPipeline/串行守卫)→ CLI/API → Web 作者面 → P* 用例 + 冒烟 + 归档 stage 1) |
| 2026-09-08 | Phase 10.1 项目目录容器 + 声明式流水线落库实施 ✅ | 按契约 [project-pipeline-foundation](phase10/design/project-pipeline-foundation.md) 落地:迁移 0013(projects/pipelines + task.project_id 只增不改)+ sqlc 重生成 + repository/project+pipeline + service(rootReady git 就绪 / CreatePipeline / **RunPipelineAs** 意图 request 覆盖 + 同 project 活跃 run 串行 409 / DeleteProjectAs 受控删)/TaskParams.ProjectID + CLI(project/pipeline;pipeline create 作者面 Web)+ /api/v1 11 路由 + Web 同步(Projects/ProjectDetail 两页 + 项目/流水线/运行三 Modal + NAV 项目·流水线) | 验证(逐条执行):gofmt/vet/build 干净;`go test -count=1 ./...` 全绿(既有零改)+ -race(service P* + server API)绿;**P1-P13 service 用例**(root 就绪三成功分支/非空非 git 零写入/重名/kind+risk 校验/按项目列表/run 挂 project+意图覆盖/串行 409/完成态重跑/project 受控删断引用+级联+磁盘不动/pipeline 删保任务/CLI actor 审计)+ **HTTP `TestAPIProjectsPipelines`**(201/400/409 映射 + run 信封 + 活跃 409 + console actor 审计);`npm run build`(tsc+vite)绿 + 重嵌 go build 绿(提交前还原占位);**二进制冒烟 G1-G11 全 PASS**(零参数 os server → setup → CLI 公司 → HTTP 建项目空目录 git init → 建流水线 → 判读档落槽 → run 建单 → 活跃再 run 409(HTTP+CLI 同源)→ 受控 DELETE 活跃 409/空闲 200 磁盘不动 → CLI read-back → 嵌入控制台服务);归档 [stage 1](phase10/stages/1.md)。实施修订:sqlc nullable FK(project_id WHERE/UPDATE → sql.NullString,repo ptrToNull);server 无 env seam → run 走 DB 默认 live 须先落 8.4 判读档(冒烟 G5b 同)。**10.2 完结**;下一工作 = 10.3(阶段计划契约通用化) |
| 2026-09-08 | Phase 10.2 schedule 调度 + ops_patrol 巡检真形态实施 ✅ | 按契约 [ops-patrol-schedule](phase10/design/ops-patrol-schedule.md) 落地(定稿门 2026-09-08 四项:① schedule 语法 = cron 五段;② 只读凭据引用后移 = 巡检落项目目录例行检查、无凭据 fail-closed 不触网(10.4+);③ 处置链 = fix+high 自动链拉同项目 bugfix;④ 调度 = 独立全局开关 schedule_poll_sec + 报告正文受控读端点内嵌阅读):新包 `internal/schedule`(cron 五段数字子集 Parse(空/off=不调度)+ Cron.Next 纯函数,dom/dow 标准 OR)+ 迁移 **0014**(task.pipeline_id 只增)/**0015**(app_setting.schedule_poll_sec DEFAULT 0)+ schema/queries/sqlc 重生成 + settings(AppSetting.SchedulePollSec/默认 0 + patch 校验)+ service(pipeline.go normalizeSchedule + CreatePipelineAs 扩 schedule 参 + UpdatePipelineScheduleAs(audit old→new)+ RunPipelineAs 挂 PipelineID + ListScheduledPipelines + 删流水线 ClearTaskPipeline 断引用;patrol.go runPatrol:approval 门→audit patrol_start→报告 `<root>/patrol/<taskID>.md`(scripted 确定性 fixture 真落盘 / live delegatePatrol 只读简报)→ **git add -- patrol/<file> + commitDelegation**→audit patrol_delegate→OS 读回(限长)→判读 verdict(scripted=OS_SCRIPT_PATROL 仅测试 seam;live=frontier 兜底;不可解析 engFail 不默认绿)→CompleteTask(result `patrol:<rel>\|ok\|severity\|action\|<summary首行>`)+audit patrol_complete→非 ok 处置(notifyCompany best-effort;fix&high&同项目 active bugfix→先完成再 RunPipelineAs(bugfix, summary, actor=system:patrol_chain)audit patrol_chain;其余 audit patrol_manual 留人))+ judge.parsePatrolVerdict + execution runClaimed 分流(kind==ops_patrol→runPatrol;流水线删/≠patrol→runEngineering 兜底)+ task.go TaskParams.PipelineID + 双清;server:SetSchedulePoll/ScheduleEnabled/ScheduleLoop(poll≤0 即返)/scheduleOnce(到点 RunPipelineAs(actor=system:schedule),成败均 advance 去重不双发,重启不补历史命中)+ settings_api.go globalSettingsDTO **补 schedule_poll_sec 回显**(冒烟抓出只写不回)+ pipelines_api.go(PUT /pipelines/{id} 改调度;GET /projects/{projectID}/patrol/{taskID} 报告受控读正文,路径服务端推导无客户端入参,64KiB 截断 truncated 标记)+ CLI serverconfig 落 SetSchedulePoll(boot 日志显开关)+ Web 同步(cron.ts 前端校验同子集 + PipelineCreateModal 加 Schedule 输入 + ProjectDetail 流水线「调度」列/「改调度」Modal + runs「形态」列 kind 徽标 + 裁决解析列 + 报告纯文本抽屉 + Settings 定时调度行;无 markdown 渲染面) | 验证(逐条执行):gofmt(本次改动干净,pre-existing 3 文件未碰)/go vet/go build 干净;`go test -count=1 ./...` 全绿(既有零改)+ `-race`(service/server/schedule)绿;**S1-S11**(cron 建/改校验合法表含 `0 0 * * 7`=周日、非法含 4 段/名字/@daily→ErrInvalid;S2 Next 跨月跨年/dom·dow OR 属 cron_test;S3 UpdatePipelineScheduleAs audit old→new;S4 调度 actor=system:schedule 建 task.pipeline_id;S5 scripted 巡检报告**真落盘 + OS git commit 归 clean + git log os-patrol 提交 + patrol_start/delegate/completo 审计齐**;S6 ok 无通知无链;S7 high+fix+active bugfix→先完成 patrol 再链拉(desc=summary/同 project/actor=system:patrol_chain,两 run 齐);S8 缺 bugfix/低危→notify+manual 不建;S9 malformed+写失败(mkdir patrol dir)→failed 不绿;S10 分流兜底;S11 删引用双清+磁盘仍在)+ **Srv-1..4**(PUT schedule 200+read-back+audit human:console/非法 400/不存在 404;ops_patrol 带 cron 建 201+run 信封 task.pipeline_id+活跃 409;scheduleOnce 单 tick(cron `* * * * *`,scheduleNext 预置**已过整分边界**)触发建单+audit system:schedule+不双发+QueueWork 关不影响(task pending)+SetSchedulePoll(0)→ScheduleLoop 即返;GET patrol 完成巡检 200 正文/超大报告 truncated=true 截到 64KiB/非巡检·未完成 400/project 不匹配 404)+ E1 扩(GET 默认 schedule_poll_sec=0 / PUT 60 回显);`npx tsc --noEmit` + `vite build` + `make ui` 重嵌 go build 绿(提交前还原占位 index.html);**二进制冒烟(实跑 bin/os)**:零参数 os server :8787(fresh DB)+healthz+GET / 嵌入新 SPA → 建公司 → HTTP 建项目(nonexistent root OS mkdir+git init)→ 建 ops_patrol 带 cron `0 9 * * *` 201 → PUT 非法 `61 * * * *` 400 带指引/合法 `0 9 * * 1-5` read-back → project tasks 空 → GET patrol 缺失 task 404 → settings 回显 schedule_poll_sec:0 → PUT 60 回显+GET 持久化(冒烟抓出 DTO 漏回显已修) → run 无判读档 8.4 硬失败文案 | 两迁移(0014/0015);无 go.mod 变更。实施修订:Srv-3 preseed 语义 = 已过整分边界(Truncate(minute),非 now-1min 任意偏移,cron 只整分命中);globalSettingsDTO 漏 schedule_poll_sec → 补字段+toGlobalSettingsDTO+E1 回归;server 无 env seam → Srv-4 用 run 建单→OS 写报告真落盘→CompleteTask(patrol: 前缀)模拟 scripted 完成巡检;scripted 离线端到端(到点→报告落盘提交→读回)由 S5/Srv-3/Srv-4 用例覆盖。live 真 claude 巡检 + 只读凭据注入 + 主机 allowlist 归 10.4+。**10.2 完结**;下一工作 = 10.3 实施契约(阶段计划契约通用化:计划落库/审批 + 逐阶段 I/O 验收 + 机械验证优先 D4) |
| 2026-09-08 | Phase 10.3 契约定稿(阶段计划契约通用化) | 实施契约 [phase-plan-contract](phase10/design/phase-plan-contract.md) 定稿:run 计划账本 = 新表 task_plan/task_plan_phase(迁移 **0016** 只增;plan 最小 FK task → 项目/流水线删除语义零新增)逐阶段记录 目标/分配器/状态/证据;双形态共一账本(patrol **upfront** 建单预铺 5 阶段可先审 / engineering **grow** 驱动真实回合边界 append,插桩不改控制流);计划审批 = **增强既有 risk=high/策略单门**(无新状态机/无 pipeline 字段;waiting_approval 时 Web 审批页随单展示预铺计划,批准=照计划执行);逐阶段 I/O 验收 + **patrol 判读前 OS 自跑机械卫生预检层**(pre/post porcelain 快照对账,委派越界残留→accept fail,通过才进判读 = D4 首个 OS 直跑真相位);GET /tasks/{id}/plan 只读端点;登记 [design README](phase10/design/README.md) | 契约定稿门三项决策通过(AskUserQuestion,均按推荐):① 覆盖面 = **双形态共一账本**(patrol 预铺可先审 + engineering 执行记账,表/门同一套);② 计划审批 = **增强既有单门**(审批页随单展示预铺计划,先审后干);③ D4 落点 = **patrol 判读前 OS 机械卫生预检**(确定性零网关,残留→fail 不经判读) | 未写码;下一工作 = 按契约实施(迁移 0016/schema/queries/sqlc/repository plan.go → service ensurePlan·runPatrol 翻状态+机械层·runEngineering grow 插桩 → GET /tasks/{id}/plan → Web TaskDrawer 计划段 + ProjectDetail 计划入口 + Approvals/DecideModal 随单展示计划 → T1-T6/Tsrv-* 用例 + 全量验证 + 归档 stage 3 + commit) |
| 2026-09-08 | Phase 10.3 run 计划账本通用化实施 ✅ | 按契约 [phase-plan-contract](phase10/design/phase-plan-contract.md) 落地(定稿门 2026-09-08 三项:① 双形态共一账本 = patrol upfront 预铺可先审 / engineering grow 执行记账;② 计划审批 = 增强既有 risk 门,审批页随单展示预铺计划先审后干;③ D4 机械优先 = patrol 判读前 OS 自跑机械卫生预检层):迁移 **0016**(task_plan/task_plan_phase 只增不改;plan 最小 FK task → 项目/流水线删除沿用断引用、无级联;phase UNIQUE(plan_id,seq))+ schema/queries(sqlc regen)+ `internal/plan/model.go` + repository/plan.go(GetTaskPlanByTask(3 值)/CreateTaskPlan/ListPlanPhases/CreatePlanPhase/UpdatePlanPhase(列指针)/SetPlanUpdated)+ service/plan.go(ensureRunPlan 幂等预铺 5 条 pending seq1..5(patrol)/grow 只建 plan 行;patrolPlanTemplate 5 阶段模板;appendPlanPhase title 去重幂等 + 终态补时间戳;ledgerAppendRound round R 写→测→审三行;ledgerAppendSplit/Ask;loadRunLedger/lgStart(仅 pending→running)/lgFinish(目标态同 → no-op))+ pipeline.go RunPipelineAs 建单同步 ensureRunPlan + delegate.go gitPorcelainSet(pre/post 快照集合)+ patrol.go 每真实边界翻 upfront 计划 + **mechanicalPatrolCheck**(报告非空 + porcelain post−pre 仅允许空,委派前用户脏项在 pre 内不误伤;残留 → phase3.fail + engFail 不经判读)+ driver.go grow 插桩(round 完成/needs_changes/熔断 fuse/评审不可解析 均记账,纯副作用不改控制流)+ plan_driver.go split/ask 记账 + api.go GET /tasks/{id}/plan 只读端点(未知 404/无计划 plan:null)+ Web 同步(types+endpoints getTaskPlan + **PlanBlock.tsx** 新(PlanTimeline 时间线 + mode="decide" 先审后干)+ TaskDrawer「计划」段 + DecideModal 决策上方随单展示 + ProjectDetail runs 行「计划」动作 + dicts 中文标签) | 验证(逐条执行):gofmt(本次改动干净;digest_test/planner_test 仅 gofmt -w 空白对齐零语义)/go vet/go build 干净;`go test -count=1 ./...` 全绿(既有用例**零 body 改**)+ `-race`(service/server/storage)绿;**T1**(三形态建单即铺账:patrol upfront 5 条 pending seq1..5 矩阵/grow 无 phase/手动无 plan)+ **T2**(ok 巡检 seq1..4 全 ok + 证据、seq5 skipped「ok — no disposition」;finding-high 链拉 seq5.dispose ok note 链 → 既有 S5-S11 零 body 改)+ **T3**(残留 → seq3.fail 不判读 OS_SCRIPT_PATROL 未消费;pre 已有用户脏项不误伤;mechanicalPatrolCheck 单测空/clean/stray/pre-existing)+ **T4**(direct 3 行/needs_changes 6 行/熔断 9 行 tail fuse → waiting_approval → approve 续跑 12 行 round3 ok/split N 子任务无 plan/ask pending 行 note)+ **T5**(risk=high 先审后干 approve requeue 推进 / reject failed / medium 不 waiting)+ **T6**(project/pipeline delete 任务+plan/phase 保留、引用置空、磁盘仍在)+ **Tsrv-1**(newSetupServer 真 svc+http:patrol 预铺完成 run 200 全形状(flip 到 ok 1-4 带 evidence+起止,seq5 pending)/grow 未执行 200 phases 空/手动 plan:null/未知 404/无令牌 401);`npx tsc --noEmit` + `vite build` + `make ui` 重嵌 go build 绿(提交前还原占位 index.html);**二进制冒烟(实跑 bin/os)**:server :8787 fresh DB + healthz → setup → 建公司 → HTTP 建项目(已 git root)→ 建 ops_patrol → 控制台加判读端点(POST /endpoints + /endpoints/{id}/select tier frontier/standard)→ **run 200 建单**(8.4 落槽 reviewer=frontier/test=standard)→ **GET /tasks/{id}/plan 5 条 pending**(kind=patrol materialized=upfront)→ SPA 根服务 + 无令牌 plan 401 | 一迁移(0016);无 go.mod 变更。实施修订:ensureRunPlan 预铺**缺 seq 赋值的真实 bug**(模板行未写 seq → 5 行全 seq=0 撞 UNIQUE(plan_id,seq),修 `tpl.Seq=int64(i+1)`);server 无 env seam → Tsrv-1 用 run 建单 + store 直铺模拟完成态验读端点形状(同 10.2 Srv-4 思路);runLedger/lg 对无计划/缺 seq 行 no-op 不波及其他 run;grow 记账幂等 = title 去重 + 终态补时间戳、pending 行(ask)留空;二进制冒烟 run 返回 **HTTP 200**(10.1 起 apiOK 信封定稿语义,projects_api_test 既有断言即 StatusOK;定稿清单「run 201」措辞不精确)。live 真 claude 巡检/判读验收 + 机械层扩 engineering OS 直跑 build·test + 通用计划解释器归 10.4。**10.3 完结**;下一工作 = 10.4 实施契约(高智合成 + 预算,见方向 §八) |

| 缺口 | 归属 | 状态 | 落地日期 |
|---|---|---|---|
| — | — | — | — |
